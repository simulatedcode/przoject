'use client'

export default function HeroText() {
  return (
    <section className="fixed inset-x-0 top-0 px-6 py-8 flex flex-col md:flex-row items-start md:items-center justify-between gap-4 pointer-events-none">
      <h1>Speculative Future Memories</h1>
    </section>
  )
}
